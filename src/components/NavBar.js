import React from 'react';

const NavBar = () => (
  <nav className="nav">
    <div className="nav-left">
      <a className="nav-item">
        My Priorities
      </a>
    </div>
  </nav>
);

export default NavBar;
